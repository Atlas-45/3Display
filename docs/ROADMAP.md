# Roadmap (PoC)

1) 透明オーバーレイWindow + SceneKit表示
2) USDZ読み込み + 基本アニメーション
3) スクリーン面クリップ（ポータル演出）
4) スライダで方向/強さ制御
5) カメラ入力 → Visionで顔追跡 → 視点オフセット
6) Metalでポスト処理（輪郭/ブルーム/DOF）

次の小目標
- 1 + 2 を最短で動かす
- 画面外から“出てくる”一番強い錯視を先に作る
